//
// Created by alstrots on 8/11/17.
//

#include "trajectory_builder.h"

position trajectory::get_position_at(const double &delay) {
	throw std::exception();
	return position();
}

trajectory trajectory_builder::build_trajectory(vector<position> sparse_points) {
	throw std::exception();

	//
//						double next_s = car.s_ + dist_inc * (i + 1);
//						double next_d = target.d();
//						auto next_xy = waypoints.getXY(next_s, next_d);

	return trajectory();
}

